# API Configuration
# 
# IMPORTANT: This file is for LOCAL DEVELOPMENT ONLY (when running without Docker)
# When using Docker Compose, the VITE_API_URL is set automatically via build args
# in docker-compose.yml to use the backend service name: http://backend:3000
#
# For local development (when running backend locally with npm run dev):
VITE_API_URL="http://localhost:3000"

# For Docker (if using nginx proxy - not recommended):
# VITE_API_URL="http://localhost:3001"

# For Docker (direct backend connection - used automatically in docker-compose.yml):
# VITE_API_URL="http://backend:3000"
